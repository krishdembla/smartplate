# R artifacts
.Rhistory
.RData

# macOS
.DS_Store
__MACOSX/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/

# Jupyter
.ipynb_checkpoints/

# Large binary model weights (not the 14 MB SegFormer-B0 checkpoint)
*.bin
*.npy
*.pt

# Smoke test training history (not meaningful — real run history lives in Drive)
code/checkpoints/train_history.json

# Custom plate photos (personal test images, not part of the project)
code/assets/custom_plates/

# Dataset cache (auto-downloaded by notebooks)
~/.cache/huggingface/

# Claude assistant files
.claude/

# LaTeX build artifacts
report/*.aux
report/*.log
report/*.out
report/*.bbl
report/*.blg
report/*.synctex.gz
report/*.fls
report/*.fdb_latexmk
report/sec/*.aux
